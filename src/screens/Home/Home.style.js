import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  form: {
    marginTop: 40,
    alignItems: 'center',
  },
  container: {
    flex: 1,
    width: '100%',
    height: '100%',
  },
});
