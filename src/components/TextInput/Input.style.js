import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    borderWidth: 1,
    height: 50,
    padding: 10,
    margin: 10,
    borderRadius: 5,
    backgroundColor: 'white',
  },
});
